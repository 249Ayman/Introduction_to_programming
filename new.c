#include <stdio.h>
#include <string.h>
#include <stdbool.h>

bool add(int a, int b, int c);
int main(void){
    add(10,20,5);
};

bool add(int a, int b, int c){
    if(a,b,c >0 && a,b > c){
        return (true);
    }
    else {
        return (false);
    }
}
